Notas de aprendizaje de este proyecto

1. Para agregar información al repositorio se tiene que trabajar en el terminal

1. git add .  Para agregar el repositorio
2. git commit -m "comentarios que se van a realizar" este comando permite mirar los cambios realizados ...
entre cada versión y agregar un comentario para el repositorio
3. git push -u origin main: sube los datos al repositorio. 

